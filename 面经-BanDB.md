# 面经：BanDB

## 项目简介（简历可用，1～2 句）

基于 Go 从零实现的边缘采集 Key-Value 数据引擎，支持自研 TCP 二进制协议、gRPC 入口、LSM 存储、WAL 崩溃恢复、Raft 写复制、采集入口过滤和范围查询谓词下推。项目重点解决高频边缘数据本地优先落盘、落盘前轻量治理、弱网下只回传命中数据切片，以及写路径持久化和性能之间的取舍。

## 简历 bullet（4～6 条）

- 实现 standalone 写路径的 WAL 持久化与崩溃恢复，补齐写确认后的恢复契约，并通过重启恢复测试覆盖已刷盘和未刷盘数据。
- 优化 WAL 写入为 group commit，将并发写共享一次 fsync，在本机 gRPC 压测中把 PUT 从 225 qps 提升到 6,087 qps/22,231 qps（不同并发下）。
- 开发基于跳表 MemTable、SSTable 块索引、分区 Bloom Filter 和墓碑删除的 LSM 存储路径，支持点查、刷盘、重载恢复和流式归并。
- 接入字节级背压组件，对未刷盘热数据设置内存预算；在压力测试中证明 inflight 可精确卡到 16MiB，避免高频写入撑爆内存。
- 新增采集入口过滤钩子与边缘范围查询能力，支持畸形帧丢弃、时间戳回退拦截、字段脱敏、JSON 字段谓词下推和命中切片回传。
- 补充 benchmark、指标快照和迭代复盘文档，用实测数据归因 fsync、MemTable 背压和 SSTable 元数据增长等瓶颈。

## 面试问题（按频率排序）

### Q1（频率：高）[后端/项目总览]

**面试官问题**：你这个项目解决什么问题？为什么不是一个普通的 KV 存储练手项目？

**口播版（第一人称，≥150 字，完整 STAR）**

我做这个项目的场景是边缘设备会持续产生高频多模态数据，比如 IMU、相机帧元信息或者采集状态。它和普通业务 KV 的区别在于，边缘侧网络弱、内存有限，但采集入口又不能因为中心侧不可用就丢掉关键数据。所以我的任务不是单纯实现 put/get，而是做一个本地优先的采集缓冲层：写入先在本地落盘，落盘前可以做校验、脱敏和丢弃畸形帧，之后还能在边缘侧按时间范围和字段谓词筛出关键切片。实现上我把系统拆成入口层、服务层、存储层和可选复制层：入口层负责二进制协议和钩子，服务层统一写读接口，存储层用 LSM 思路吸收高频写，复制层用 Raft 支持写日志复制。结果是这个项目能围绕持久化、写性能、内存边界、弱网回传这些工程约束展开，而不是只停留在内存 Map。

**追问 1**：你会怎样向面试官画这个项目的核心链路？

**口播版（≥150 字）**：我会先从一条写请求讲起，因为写路径最能体现系统约束。客户端通过 TCP 二进制协议或 gRPC 进入服务，入口层先做协议解析和可选的采集过滤；如果是 standalone 模式，服务层先把写命令追加到预写日志并同步落盘，然后再写入内存表；如果是复制模式，写命令先进入共识日志，提交后由状态机应用到存储引擎。存储层的热数据先进入有序内存结构，达到阈值后切成不可变快照刷成磁盘表。读路径则先查最新内存表，再查正在刷盘的快照，最后查磁盘表，并用范围元信息、Bloom Filter 和块索引减少无效磁盘读。这样画的好处是能自然解释性能、恢复、一致性和查询边界，不会把项目讲成散乱功能清单。

**追问 2**：这个项目目前最应该诚实说明的边界是什么？

**口播版（≥150 字）**：我会主动说明两个边界。第一，范围查询当前主要覆盖热数据，也就是还在内存表里的窗口；已经刷到磁盘表的历史数据范围扫描在路线图里，还不能把它包装成完整时序数据库。第二，Raft 复制在项目里更像教学型和项目型实现，覆盖了选举、心跳、日志复制、持久化和快照，但我不会把它说成生产级分布式数据库。我的表达会落在“我理解写复制和状态机 apply 的核心机制，并把它接进了 KV 写路径”。这种说法更稳，因为面试官追问边界时，我能讲清哪些已经有测试和实测，哪些还需要更多多节点故障注入、长稳压测和生产化治理。

### Q2（频率：高）[后端/持久化]

**面试官问题**：standalone 模式下，你怎么保证写成功返回后崩溃不丢数据？

**口播版（第一人称，≥150 字，完整 STAR）**

我面对的问题是，如果只把数据写进内存表，请求返回成功后进程崩溃，用户确认过的数据就会丢，这违反了存储系统最基本的持久化契约。所以我在 standalone 模式里把写路径改成先写预写日志，再写内存表。具体动作是：服务层收到 Put 或 Delete 后，先把操作编码成日志记录追加到文件，并等待 fsync 成功；只有日志已经持久化，我才把它应用到 MemTable。重启时再顺序读取日志，把每条记录按 Put 或 Delete 重新应用到内存结构。这里重放采用幂等盲写，读到残缺尾部记录就停止，避免崩溃时撕裂写导致恢复失败。结果是 `Append` 返回成功就代表这条写至少可以通过日志恢复，存储层从“写内存很快但会丢”变成了有明确崩溃恢复语义的写路径。

**追问 1**：为什么一定要先写 WAL，再写 MemTable？反过来可以吗？

**口播版（≥150 字）**：反过来先写 MemTable 再写 WAL，在正常路径上看起来也能工作，但崩溃窗口会变得不可接受。比如我先把数据写进内存表，还没来得及写日志，服务如果已经返回成功或者后续逻辑误认为写入完成，这条数据一旦进程崩溃就没有任何恢复来源。先写 WAL 的本质是 write-ahead logging，也就是把恢复事实先落到稳定存储，再更新易失内存状态。这个顺序还有一个细节：WAL append 成功但 MemTable 还没写完之间也有窗口，所以项目里用一把读写锁把这两步和 checkpoint 协调起来，避免 checkpoint 在中间截走日志。我的目标不是消灭所有中间状态，而是让每个中间状态在崩溃恢复时都有可解释的安全边界。

**追问 2**：WAL 重放时遇到半条记录怎么办？要不要报错？

**口播版（≥150 字）**：我会把半条尾部记录当作崩溃时的撕裂尾写处理，而不是直接让恢复失败。原因是 WAL 写入文件时，进程可能在写完 header、key 或 value 的任意位置崩溃，文件尾部出现不完整记录是符合预期的故障形态。我的处理策略是顺序读取完整记录，只要读 header 或 body 发现 EOF 或残缺，就停止重放并返回成功；之前完整 fsync 过的记录已经能恢复，最后这条不完整记录没有成功确认，不应该影响整体启动。这个策略的代价是 WAL 没有 CRC 校验，不能识别所有静默损坏；如果继续演进，我会加记录级校验和长度边界，区分“正常撕裂尾写”和“中间损坏”，这样恢复语义会更严谨。

### Q3（频率：高）[后端/性能优化]

**面试官问题**：你们写性能优化最核心的一次是怎么做的？

**口播版（第一人称，≥150 字，完整 STAR）**

最核心的一次优化是 standalone 写路径的 group commit。场景是我用 gRPC 入口压测时发现 GET 有一万六千多 qps，但 PUT 只有 225 qps，P50 到 203ms，这个数字非常反常。我的第一步不是猜锁竞争，而是先测本机单次 fsync，结果约 3.93ms，也就是每秒 254 次左右，和 225 qps 高度吻合。根因就很清楚：每条写都独立 fsync，吞吐被磁盘同步次数封顶。我的动作是把 WAL 改成单一后台 flushLoop，所有写请求先进 channel，flushLoop 攒当前队列中的一批记录，顺序写入后只 fsync 一次，再把同一个结果返回给批内所有等待者。结果在同样持久化语义下，50 并发 PUT 提升到 6,087 qps，200 并发提升到 22,231 qps，优化收益来自把 N 次 fsync 摊销成 1 次。

**追问 1**：group commit 会不会牺牲数据安全？

**口播版（≥150 字）**：我的实现目标是提升吞吐，但不改变“写返回成功即已落盘”的语义。它不是定时异步刷盘，也不是先返回再慢慢 fsync；每个写请求都会带一个 done channel，进入批次后等待 flushLoop 写文件并 fsync，只有 fsync 成功，等待者才会收到 nil 并向上返回成功。如果批次里任何写或 fsync 出错，同一批请求都会收到错误。换句话说，group commit 改变的是多个请求共享一次同步的组织方式，不是确认时机。它的风险主要在延迟和批次控制：并发低时批次可能很小，收益有限；并发特别高时批次不能无限大，所以我设置了单批最大条数，避免极端突发下一个批次拖太久。

**追问 2**：你怎么证明瓶颈真的是 fsync，而不是网络或 JSON 编码？

**口播版（≥150 字）**：我用了分层对照的方法。首先 GET 走同样的 gRPC 入口和服务层，但不落盘，能达到 16,806 qps，说明网络和服务框架不是 225 qps 的主要限制。其次我单独测了本机 fsync，大约 254 次每秒，和 PUT 改前 225 qps 非常接近，这个数量级吻合比主观猜测更可靠。然后我做了 group commit，只改 WAL 同步方式，不改变外层协议和存储语义，PUT 在 50 并发下提升到 6,087 qps，200 并发到 22,231 qps。如果瓶颈在网络或 JSON，这个改动不会带来接近两个数量级的提升。最后我把结果写进迭代文档，保留环境、参数和复现命令，避免只说“优化了性能”。

### Q4（频率：高）[后端/LSM]

**面试官问题**：你这个 LSM 存储路径是怎么实现的？

**口播版（第一人称，≥150 字，完整 STAR）**

我实现 LSM 的目标是让高频写先走内存里的顺序友好结构，再异步刷成磁盘上的不可变文件。具体来说，写入先进入 MemTable，内部用跳表保持 key 有序，这样点查和范围扫描都有基础。MemTable 采用 active 和 dirty 双表：active 接收新写，达到阈值后切成 dirty 快照，后台把 dirty 按 key 顺序写成 SSTable，同时创建新的 active 继续接收写入。SSTable 不是简单的裸文件，它包含有序数据段、块索引、Bloom Filter 和 footer，读的时候会按新到旧查找，先用 key 范围过滤，再用 Bloom 否决，再二分块索引定位目标块。删除用墓碑表达，避免旧文件里的值被复活。这个设计的结果是写路径很轻，读路径也有逐层过滤，不会退化成每次全文件扫描。

**追问 1**：为什么 MemTable 用跳表？不用 map 会怎样？

**口播版（≥150 字）**：如果只支持点查，map 会更简单，但这个项目的存储层还需要范围扫描和有序刷盘。跳表的优势是它既能接近对数复杂度地查找和插入，又天然维护 key 的有序链表。这样 Flush 时从第 0 层顺序遍历就能得到有序 entries，直接写成 SSTable；Scan 时也能从第一个大于等于 start 的节点开始向后走，直到超过 end 停止。如果用 map，我还需要每次刷盘或扫描时额外排序，频繁高频写下会把成本推迟到刷盘阶段。跳表还有实现相对简单、并发控制清晰的优点。项目里通过 MemTable 外层读写锁保证线程安全，跳表内部不再单独做复杂并发结构，降低实现风险。

**追问 2**：active/dirty 双表有没有数据一致性风险？

**口播版（≥150 字）**：有风险，所以我把切表和刷盘拆成两个阶段处理。切表阶段持锁完成：如果当前没有 dirty，就把 active 变成 dirty，再创建一个新的 active；这个过程很短，写入只短暂等待。刷盘阶段在锁外执行，把 dirty 的有序数据写入 SSTable。读路径会先查 active，再查 dirty，最后查 SSTable，所以刷盘期间旧数据仍然可见。真正需要注意的是刷盘失败：如果失败后直接丢掉 dirty 或被下一次切表覆盖，就会丢未落盘数据。项目里的处理是 dirty 保留不清空，记录错误后重新触发 flush，只有 SSTable 写入并 fsync 成功后才把 dirty 置空并释放背压信用。这样牺牲一点复杂度，换来失败路径的数据安全。

### Q5（频率：高）[后端/删除语义]

**面试官问题**：LSM 里删除为什么要用墓碑？你项目里怎么处理的？

**口播版（第一人称，≥150 字，完整 STAR）**

我一开始处理删除时重点考虑的是 LSM 的多层历史版本问题。数据一旦刷到 SSTable，旧文件是不可变的，不能像内存 map 一样直接把所有历史副本都删掉。如果删除只是在当前内存结构里物理移除，那么读路径查不到内存里的新状态后，会继续下穿到旧 SSTable，结果把已经删除的值读出来，这就是删除后复活。我的做法是把删除也建模成一条写入，只是 value 用 nil 表示墓碑。读路径一旦命中墓碑，就返回 key 不存在，不再继续向更旧的文件查找；刷盘时也把墓碑编码成特殊 value length，重启和归并时都能保留删除语义。结果是 Delete 变成幂等盲写，既适合 WAL 重放，也适合后续 compaction 清理旧版本。

**追问 1**：墓碑会不会越来越多？什么时候可以清理？

**口播版（≥150 字）**：墓碑确实会带来空间放大，这是 LSM 删除语义的典型代价。它不能在写入时立即清理，因为更旧层级的 SSTable 里可能还有同名 key，如果过早丢掉墓碑，旧值会在读路径或重启后复活。合理的清理时机是在 compaction 能证明某个 key 的所有更旧版本都已经被这次归并覆盖之后，墓碑才可以被丢弃。当前项目主要保留墓碑语义，确保删除正确性优先；如果继续演进，我会在分层 compaction 中引入更严格的层级边界判断，比如当合并到最后一层或确认不存在更旧文件覆盖范围时再清理墓碑。面试里我会强调：先保证不会读错，再谈空间回收。

**追问 2**：nil value 和空 value 怎么区分？

**口播版（≥150 字）**：这个边界很重要，因为空字节切片是合法业务值，而 nil 在项目里表示删除墓碑。如果把两者混在一起，用户写入空值可能会被误判成删除，或者删除在重放时变成普通空值。我的处理是内存里用 `Value == nil` 表示墓碑，非 nil 的空切片表示普通写入；写 SSTable 时用一个正常 value 长度不可能取到的哨兵值表示墓碑，读出来再还原为 nil。WAL checkpoint 重写时也要保留这个语义，看到 nil 就编码为 Delete 操作，看到空切片则仍然是 Put。这个细节能体现我不是只实现 happy path，而是把删除、空值、恢复和重写这些边界串起来考虑。

### Q6（频率：高）[后端/崩溃恢复排障]

**面试官问题**：你项目里有没有遇到过隐藏很深的数据恢复 bug？

**口播版（第一人称，≥150 字，完整 STAR）**

有一次非常典型。场景是我给 standalone 写路径做 checkpoint 和恢复测试时，用很小的 MemTable 阈值逼数据刷到 SSTable，然后停机重启读回，发现 50 个 key 只恢复了 5 个，已刷盘的大部分数据都读不到。我的任务是先判断这是新改动引入的问题，还是旧路径就有的缺陷。我写了一个不含 checkpoint 的隔离测试，结果同样复现，说明这是旧 bug。继续定位时我打印重载后的 SSTable 元信息，发现 MaxKey 变成空串，读路径用 MinKey/MaxKey 范围过滤时直接把文件跳过。根因是加载元信息时顺序扫描扫过了数据段边界，把后面的块索引和 footer 当成记录解析。修复方式是新格式文件直接从块索引最后一项取 MaxKey。结果恢复测试从 5/50 变成 50/50，这个问题也让我更重视“真的重启一次”的测试。

**追问 1**：这个 bug 为什么之前没有被发现？

**口播版（≥150 字）**：它之前被测试场景掩盖了。原有恢复测试使用很大的 MemTable 阈值，数据基本还留在内存和 WAL 里，没有强制刷成 SSTable 后再重启。因此测试验证的是“WAL 能不能重放热数据”，但没有验证“已经刷盘的数据能不能通过 SSTable 元信息被重新发现”。这类问题的本质是测试没有覆盖状态迁移后的恢复路径：数据从 WAL 到 MemTable，再从 MemTable 到 SSTable，每个阶段的恢复来源不同。我的修复不只是改代码，还补了小阈值多轮刷盘、停机、同目录重载、逐个 key 读回的测试。这样测试从单一热路径变成覆盖冷数据路径，才真正证明 LSM 持久化可用。

**追问 2**：你从这个问题里学到的工程经验是什么？

**口播版（≥150 字）**：我最大的经验是，存储系统不能只靠正常运行时读写测试，必须测试状态跨越和重启后的可见性。很多 bug 在进程内读写时不会暴露，因为内存结构还在；一旦重启，系统只能依赖磁盘元信息、日志和文件格式恢复，任何格式边界错误都会变成数据丢失。第二个经验是，优化或新增特性经常会挖出旧 bug，这时不能急着把锅归到新代码上，要先做隔离复现。第三个经验是，文件格式一旦尾部追加索引、Bloom 或 footer，所有老的顺序扫描逻辑都要重新审视，明确数据段结束位置。面试里我会把这件事讲成一次从现象到隔离、定位、修复、回归测试的完整排障链。

### Q7（频率：高）[后端/WAL checkpoint]

**面试官问题**：WAL 一直追加会无限变大，你是怎么处理的？

**口播版（第一人称，≥150 字，完整 STAR）**

WAL 只追加不清理会在高频写场景下持续膨胀，即使很多数据已经刷成 SSTable，历史日志也还留在文件里。我的任务是让 WAL 大小有界，同时不能破坏崩溃恢复。我做的方案是 checkpoint：每累计一定次数写入后，暂停和写路径冲突的部分，取当前未刷盘热数据的快照，包括 active 和 dirty 表以及删除墓碑，然后把 WAL 重写为这份最小必要状态。这样已经落到 SSTable 的历史记录不需要再保留，而未刷盘数据仍然能靠新 WAL 恢复。这里的关键是用读写锁协调写入和 checkpoint，写入持读锁包住 WAL append 和 MemTable apply，checkpoint 持写锁等待所有在途写静默。结果是 WAL 不再只增不减，恢复契约也保持清晰。

**追问 1**：为什么 checkpoint 不能直接 truncate 原 WAL 后重写？

**口播版（≥150 字）**：直接 truncate 再重写有一个严重崩溃窗口：如果进程在 truncate 成功后、重写并 fsync 前崩溃，原 WAL 已经被清空，新 WAL 又不完整，而这份 checkpoint 快照代表的正是尚未刷到 SSTable 的热数据，丢了就会丢已确认写。所以我把重写改成原子替换：先把新记录完整写到临时文件并 fsync，再用 rename 覆盖原文件，最后 fsync 父目录保证目录项持久。这样崩溃时系统要么看到完整旧 WAL，要么看到完整新 WAL，不会看到一个被截断的中间态。这个修复让我意识到，优化空间占用不能牺牲持久化契约，尤其是存储系统里任何“清理”动作都要先过崩溃模型。

**追问 2**：checkpoint 和并发写之间的竞态是什么？

**口播版（≥150 字）**：最危险的竞态是写入不是一个单一原子动作。standalone 写路径先 WAL append，再写 MemTable；如果 checkpoint 刚好在这两步之间拿快照，它可能看到 WAL 里有一条已经追加的记录，但 MemTable 快照里还没有这条数据。随后 checkpoint 把 WAL 重写成快照内容，就会把这条已落 WAL 但未进快照的写丢掉。我的处理是用读写锁把两步罩在一起：普通写持读锁，允许多个写并发进入，group commit 仍能发挥作用；checkpoint 持写锁，必须等所有读锁写完成后才能快照和重写。这样 checkpoint 看到的是一个没有在途写的稳定视图，避免把中间态固化成丢数据。

### Q8（频率：高）[后端/背压]

**面试官问题**：高频摄入时内存撑爆怎么办？你怎么做背压？

**口播版（第一人称，≥150 字，完整 STAR）**

我在高频摄入压测中发现一个问题：短跑几秒看起来写入很快，但 60 秒复测时 heap 会持续增长，说明系统没有稳定内存边界。我的任务是先区分内存增长来自未刷盘热数据，还是来自 SSTable 元数据。第一步我给 MemTable 写路径接入字节级信用池：每次 Put 或 Delete 先按 key/value 大小申请信用，超过预算就触发 flush 并阻塞等待，Flush 成功后按 dirty 表实际字节数释放信用。这样当写入速度超过刷盘速度时，系统选择限速写入，而不是无限堆内存。实测在刻意关闭条数触发、让 MemTable 成为瓶颈时，16MiB 预算能把 inflight 精确卡在 16MiB，证明背压机制有效。后来我也诚实记录：默认小 value 场景的 heap 增长更多来自 SSTable 元数据和 Bloom 常驻，背压不是所有内存问题的解药。

**追问 1**：为什么按字节背压，而不是按条数背压？

**口播版（≥150 字）**：按条数背压实现简单，但它无法表达真实内存风险。一条 64B 的传感器状态和一条几 MB 的图像元数据，如果都算一条，内存压力完全不同。我的目标是控制未刷盘数据占用，所以按 key 和 value 的字节增量申请信用更准确。实现时还要处理覆盖写：如果新值比旧值小，不能一直按完整大小累加，否则信用会泄漏；所以跳表插入会返回实际 byteSize 变化量，写路径再归还多申请的部分。删除也要按墓碑占用估算。这个设计本质上是把 MemTable 当成一个有预算的缓冲区，超过预算就让生产者等待消费者刷盘，而不是继续无界接受输入。

**追问 2**：背压带来的副作用是什么？

**口播版（≥150 字）**：背压的副作用是它会把存储层不可持续的压力显式反馈给上游，吞吐可能下降，尾延迟可能升高，甚至有界队列会开始丢帧。但这不是缺陷，而是系统从“悄悄 OOM”变成“可观测地限速”。在我的压测里，当开启预算后，如果 producer 速率超过消费者可持续写入能力，队列可能溢出，这说明上游速率已经超过系统承载，需要降采样、扩大队列、提升 flush/compaction 能力或做策略性丢弃。面试里我会强调背压不是为了让所有请求更快，而是为了让资源有边界。真正生产化时，还要配合指标，比如背压阻塞次数、未刷盘字节数、丢帧数和 flush 耗时，才能决定是扩容还是降级。

### Q9（频率：中）[后端/SSTable 读优化]

**面试官问题**：SSTable 读路径如何避免每次全文件扫描？

**口播版（第一人称，≥150 字，完整 STAR）**

如果每次 GET 都扫描所有 SSTable，文件数一多读延迟会快速退化。所以我在 SSTable 文件里增加了几层过滤和定位结构。第一层是文件元信息，每个文件记录 MinKey 和 MaxKey，读某个 key 时先判断它是否可能落在这个范围内。第二层是 Bloom Filter，尤其针对不存在的 key，可以在不读数据块的情况下快速否决。第三层是块索引，每隔固定数量的有序 entry 记录一个块的最后 key 和块偏移，读时用二分找到目标块，只扫描这个小块。文件尾部通过 footer 记录索引位置，重启时可以加载索引和 Bloom。结果是读路径从“所有文件全量扫”变成“新到旧文件逐层过滤、命中候选后定位块”，这更符合 LSM 读放大的控制思路。

**追问 1**：Bloom Filter 为什么做成分区的？

**口播版（≥150 字）**：项目里的 key 往往带有命名空间前缀，比如某类传感器、设备或数据域。普通 Bloom 把所有 key 混在一起，虽然也能过滤不存在的 key，但不同命名空间会相互污染误判率，而且长前缀会参与重复哈希。分区 Bloom 的思路是先按第一个分隔符拆出命名空间，每个命名空间维护独立过滤器，插入和查询时只对后缀做哈希。如果查询的命名空间根本不存在，可以直接返回 false；如果存在，也只在对应分区判断。这个设计的收益是更贴近边缘采集 key 的组织方式，负查询更容易被提前否决。代价是分区和过滤器元数据会常驻内存，文件数很多时需要靠 compaction 或缓存策略收敛。

**追问 2**：块索引和 Bloom 分别解决什么问题？

**口播版（≥150 字）**：Bloom Filter 解决的是“这个文件大概率有没有这个 key”的问题，尤其擅长负查询；块索引解决的是“如果可能有，我应该读文件的哪一段”的问题。两者不能互相替代。只有 Bloom 时，命中或误判后仍可能要扫描较大范围；只有块索引时，每个候选文件仍要打开并二分索引，对明显不存在的 key 也会产生开销。项目里读路径先用范围元信息做粗过滤，再用 Bloom 快速否决，最后用块索引定位小块扫描。这样可以把大量不存在的查找挡在磁盘数据段之外，又能让存在的查找避免从文件头扫到目标位置。面试回答时我会把它们描述成两级优化：一个减少候选文件，一个减少文件内读取范围。

### Q10（频率：中）[后端/Compaction]

**面试官问题**：多个 SSTable 怎么合并？如何保证同一个 key 保留最新版本？

**口播版（第一人称，≥150 字，完整 STAR）**

SSTable 是不可变文件，频繁 Flush 会产生很多小文件；如果不合并，读放大和元数据内存都会越来越严重。所以项目里实现了流式归并。我的做法是为每个源 SSTable 创建有序迭代器，再用最小堆做 K 路归并。堆排序规则先按 key 升序，如果 key 相同，则让来源更新的文件先弹出。弹出一个 key 后，会跳过其他源里相同 key 的旧版本，只输出最新版本。写出新 SSTable 时同步生成块索引和 Bloom Filter，保持文件格式一致。这个实现的关键收益是内存占用和参与归并的文件数相关，而不是把所有 entries 一次性读入内存。结果是 compaction 可以作为后台流程逐步减少文件数量、合并覆盖写和删除标记。

**追问 1**：K 路归并为什么要流式做？

**口播版（≥150 字）**：如果把所有 SSTable 的数据一次性读入内存再排序，代码可能简单，但和项目高频采集、低内存边缘环境的目标冲突。SSTable 文件可能很多，总数据量也可能远大于内存，物化全部 entries 会造成很高的峰值内存。流式 K 路归并只需要每个源迭代器当前的一条记录，加上一个大小为 K 的堆，内存复杂度接近 O(K)。每次弹出最小 key 后，从对应源读取下一条并重新入堆，持续写出新文件。这样 compaction 的内存更可控，也更容易和后台任务配合。项目当前还有继续演进空间，比如按层级和文件大小选择合并集合、控制后台 IO 对前台读写的影响。

**追问 2**：Compaction 现在还有什么不足？

**口播版（≥150 字）**：我会诚实说，目前 compaction 更像具备核心归并能力，但还没有完整生产级调度策略。已有代码能做有序迭代、同 key 去重、流式写出和新索引生成；但从 benchmark 文档看，小值高频写时 heap 增长仍然和 SSTable 文件数量、Bloom/索引常驻元数据有关，说明后台合并和缓存收敛还需要加强。下一步我会做几件事：第一，记录文件数、每层大小、Bloom 内存和 compaction 耗时指标；第二，明确按层级或大小触发的合并策略；第三，限制 compaction 并发 IO，避免影响前台写；第四，补充长时间压测验证文件数是否进入稳态。这样回答能避免把“能合并”夸成“已经完全解决读放大和空间放大”。

### Q11（频率：中）[后端/边缘过滤]

**面试官问题**：你说支持落盘前可编程预处理，具体做了什么？

**口播版（第一人称，≥150 字，完整 STAR）**

这个能力来自边缘采集场景：有些数据在进入存储前就应该被处理，比如畸形帧、过大 payload、时间戳回退帧或敏感字段。如果全部先落盘再离线清理，不仅浪费边缘存储，也可能扩大隐私风险。所以我在入口路由层设计了 PreHandle 钩子，写请求真正进入服务层之前先经过过滤器。过滤器会解析 PUT 负载，检查长度字段是否和实际数据一致，限制 value 大小；如果配置了时间戳单调校验，就按设备记录最近接受的时间戳，回退或重放帧直接丢弃；如果 value 是 JSON 且包含敏感字段，就改写为脱敏占位后重建请求帧。结果是项目不只是存储数据，还能在采集入口做轻量治理，贴合边缘设备本地处理的定位。

**追问 1**：丢弃请求时如何避免客户端响应错位？

**口播版（≥150 字）**：这是请求响应协议里很容易忽略的细节。如果 PreHandle 直接返回丢弃但不回响应，客户端可能还在等待这一帧的结果，后续下一条请求的响应就会被误读成上一条，整条连接都会错位。我的处理是把“丢弃也必须回唯一响应”的不变式放在路由层统一维护：过滤器只返回通过或丢弃，不直接写连接；路由的 PreHandle 看到丢弃后负责写回 dropped 状态，并停止后续 Handle。这样钩子实现者不需要关心协议响应细节，也避免多个地方重复写响应。这个设计本质上是把策略判断和协议完整性分离，保证每个请求恰好对应一个响应。

**追问 2**：时间戳单调校验有什么局限？

**口播版（≥150 字）**：时间戳单调校验在项目里是 best-effort 的入口过滤，不是严格全局顺序保证。原因是网络层有 worker pool 和 work stealing，当某个队列满时，同一连接的请求可能被分发到其他 worker，极端情况下处理顺序可能和到达顺序不完全一致。过滤器按设备维护最近接受的时间戳，可以拦截明显回退或重放的帧，但如果并发调度导致先处理后到的大时间戳，再处理先到的小时间戳，就可能把一条本来合法的迟处理帧丢掉。面试里我会把它讲成边缘入口的轻量防线，适合防明显异常，不替代端到端严格排序。如果要做强顺序，需要按设备分区串行化或在更上层引入序列确认机制。

### Q12（频率：中）[后端/范围查询]

**面试官问题**：边缘范围查询和谓词下推是怎么做的？

**口播版（第一人称，≥150 字，完整 STAR）**

项目的边缘查询目标是减少弱网回传量：不要把整段原始数据都传回中心侧，再由中心筛选，而是在边缘节点先找出命中的关键切片。实现上，SCAN 请求包含 start、end、字段名、操作符和操作数，协议层负责把这些参数编码成二进制负载。服务层拿到请求后调用存储引擎的范围扫描，在 key 的闭区间内升序遍历热数据；每条 value 按 JSON 对象解析，再执行单字段谓词，比如数值大于阈值或字段等于某个状态。只有满足谓词的条目才复制到响应里，并且设置最大返回数量，避免无谓词大范围扫描把内存撑爆。结果是查询语义从“取回全部再筛”变成“服务端筛选后只回传命中结果”。

**追问 1**：为什么当前只覆盖热数据？怎么扩展到历史 SSTable？

**口播版（≥150 字）**：当前 Scan 只覆盖 MemTable 热数据，是因为内存跳表天然有序，范围遍历实现简单且风险较低；而 SSTable 历史扫描需要合并多个不可变文件、处理同 key 多版本、墓碑、文件新旧顺序和结果去重。如果直接逐文件扫描，很容易返回旧版本或被删除的数据。扩展时我会复用类似 compaction 的 K 路归并思想：为落在范围内的 SSTable 建迭代器，按 key 有序合并，遇到相同 key 保留最新版本，命中墓碑则跳过，再和 active/dirty 的热数据做统一可见性判断。还要利用 MinKey/MaxKey 过滤文件，并对结果数量和扫描时间做上限。这样才能把历史查询做正确，而不是简单把所有文件扫一遍。

**追问 2**：谓词下推为什么只做简单表达式？

**口播版（≥150 字）**：我刻意把谓词设计成单字段、单操作符、单操作数，是为了匹配项目当前阶段和边缘场景。边缘设备资源有限，采集查询常见需求是按时间范围找某个字段超过阈值或状态等于某值，没必要一开始引入完整表达式语言。完整表达式会带来解析器、类型系统、短路逻辑、安全边界和性能优化问题，容易把项目重心从存储和采集链路带偏。当前实现支持数值比较和标量相等比较，非 JSON、字段缺失或类型不匹配都返回 false，这个失败语义简单可控。如果后续需求增加，我会先扩展为 AND/OR 的小型 AST，并给表达式复杂度和执行时间加限制，避免查询层被滥用。

### Q13（频率：中）[后端/Raft]

**面试官问题**：项目里的 Raft 写复制是怎么接入 KV 的？

**口播版（第一人称，≥150 字，完整 STAR）**

项目有两种运行模式：standalone 模式依赖存储层 WAL 保证单机恢复；raft 模式则把写命令先交给复制日志。我的接入方式是在服务层统一暴露 Write。收到 Put 或 Delete 后，如果当前启用了 Raft，就把命令编码成日志 entry，调用复制层追加；只有日志达到提交条件后，状态机 apply 循环才把命令应用到存储引擎。这样客户端写入和底层存储之间隔了一层提交语义，存储引擎本身不需要知道这条写来自单机 WAL 还是共识日志。Raft 模块里包含 follower、candidate、leader 状态，随机选举超时、心跳、日志索引、提交索引和快照元信息。我的重点是把共识日志和 KV 状态机连接起来，展示写复制的主链路，而不是宣称它已经达到生产级数据库标准。

**追问 1**：standalone WAL 和 Raft WAL 会不会重复？

**口播版（≥150 字）**：它们服务的模式不同，不能简单混为一谈。standalone 模式没有复制日志，所以必须有存储层 WAL 来保证单机崩溃后恢复未刷盘写入；raft 模式下，写入的耐久性和复制语义主要来自 Raft 日志，提交后再 apply 到状态机。因此项目初始化时会按模式选择：standalone 打开存储层 WAL 并重放；raft 模式启动 Raft，不使用 standalone 的存储层 WAL。这样做避免每条写同时写两套日志，降低实现复杂度。代价是两种模式的恢复路径不同，面试时必须讲清楚。生产系统可能还会有状态机快照、本地恢复和复制日志截断的更复杂协同，这部分当前项目只是基础接入。

**追问 2**：Raft 这块你最担心哪些边界？

**口播版（≥150 字）**：我最担心的是多节点故障场景和生产化细节。当前代码覆盖了选举、心跳、日志复制、持久化状态和快照加载，但 Raft 真正难的是网络分区、leader 切换、日志冲突回退、慢节点追赶、快照安装和长期运行下的资源回收。项目里更适合把它作为“我理解共识协议主链路，并把它接入状态机”的证据，而不是把它包装成完整生产级 Raft。继续演进时，我会重点补 3 节点集成测试：leader 崩溃后重新选主、少数派不可用时拒绝提交、旧 leader 恢复后日志对齐、快照安装后数据一致。只有这些故障注入和长稳测试足够，才有资格谈更强可靠性。

### Q14（频率：中）[后端/网络协议]

**面试官问题**：为什么还要自研 TCP 协议，不直接只用 gRPC？

**口播版（第一人称，≥150 字，完整 STAR）**

项目保留两种入口：gRPC 适合压测、联调和通用生态；自研 TCP 二进制协议则服务于更轻量、可控的边缘采集路径。我的考虑是，边缘设备上有时希望协议头简单、依赖少、payload 可直接按 key/value 或 scan 参数编码，不一定需要完整 RPC 框架。自研协议采用定长帧头加消息 ID 和 payload，路由层根据消息 ID 分发到 Put、Get、Delete、Scan。这样我可以在入口处挂 PreHandle 过滤器，也能控制 worker pool、连接生命周期和响应格式。结果是核心 KV 服务层不绑定某一种传输协议，TCP 和 gRPC 最终都汇聚到同一套服务能力。面试里我不会说自研协议一定优于 gRPC，而是强调这是为了展示协议解析、路由分发和边缘轻量入口的工程取舍。

**追问 1**：自研协议最大的风险是什么？

**口播版（≥150 字）**：最大的风险是协议兼容性、错误处理和安全边界都要自己负责。gRPC 有成熟的 IDL、序列化、错误码、连接管理和生态工具；自研协议如果长度字段校验不严，就可能出现粘包拆包错误、内存分配过大、响应错位甚至 DoS 风险。所以项目里在解析 Put 和 Scan 时会检查负载长度是否足够，响应格式也明确带状态和长度。连接建立时不主动下发欢迎消息，因为这是纯请求响应协议，未经请求的响应会让客户端误读。继续演进时，我会加协议版本、最大 payload 限制、错误码枚举、fuzz 测试和向后兼容策略。自研协议能体现底层能力，但必须承认维护成本更高。

**追问 2**：worker pool 和 work stealing 是怎么考虑的？

**口播版（≥150 字）**：网络层用 worker pool 是为了避免每个请求都无限制创建 goroutine，同时让连接请求能被有界队列吸收。默认会按连接 ID 把请求投递到专属 worker，这样同一连接在正常情况下更容易保持处理顺序；如果专属队列满了，就尝试投递到其他 worker，属于 work stealing，用来提高突发情况下的吞吐和队列利用率。它的代价是顺序语义会变弱，尤其对同一设备的时间戳校验这种逻辑，可能出现后到请求先处理的情况。所以我会把这个设计讲成吞吐和局部顺序之间的权衡：适合无强顺序要求的 KV 请求，但如果某类业务强依赖顺序，应该按 key 或设备分区固定路由，不能盲目偷取。

### Q15（频率：中）[后端/可观测与验证]

**面试官问题**：你怎么证明这些优化真的有效，而不是感觉有效？

**口播版（第一人称，≥150 字，完整 STAR）**

我主要靠分层 benchmark、指标和复盘文档来证明。比如写性能问题，我先测 GET 和 PUT 的端到端差异，再单独测 fsync 上限，最后只修改 WAL 批量同步方式并复测，形成“现象、归因、动作、结果”的闭环。高频摄入问题也一样：短跑 6 秒看起来没问题，但 60 秒复测发现 heap 持续增长，于是我把“内存有界”的说法推翻，再做字节背压并用 inflight 指标验证预算是否卡住。代码里还有零依赖指标快照，记录写入、读取、扫描、丢弃帧、写错误、背压阻塞和未刷盘字节。结果是每个关键结论都能指向测试、benchmark 或文档，而不是只说用了某个技术所以性能好。

**追问 1**：你会补哪些测试来提高可信度？

**口播版（≥150 字）**：我会按风险补测试。第一类是崩溃恢复测试：小 MemTable 阈值、多轮刷盘、停机、重启、逐个 key 读回，覆盖 WAL 和 SSTable 两种恢复来源。第二类是 WAL checkpoint 原子性测试：旧临时文件残留、不完整重写、墓碑和空值边界、重写后继续 append。第三类是协议测试：Put/Get/Delete/Scan 的编码解码、异常长度字段、丢弃响应不乱序。第四类是 Raft 集成测试：3 节点选主、leader 故障、日志追赶、快照恢复。第五类是长稳压测：记录 heap、文件数、Bloom 内存、compaction 耗时和 P95/P99。这样测试不是堆数量，而是围绕最可能导致数据丢失、内存失控和协议错位的风险设计。

**追问 2**：如果 CI 资源有限，哪些测试必须优先跑？

**口播版（≥150 字）**：CI 资源有限时，我会优先保留确定性强、能防数据丢失的测试。第一优先级是 WAL、SSTable 重载、墓碑、checkpoint 原子替换和 MemTable 刷盘失败重试，因为这些失败会直接造成确认写丢失或删除复活。第二优先级是协议编码解码和服务层 Scan/Predicate，因为它们防止请求解析和响应格式回归。第三优先级是轻量 Raft 单测，比如持久化状态、日志追加、快照元信息；多节点长稳可以放到 nightly 或手动流水线。性能 benchmark 不适合每次 CI 都卡硬阈值，因为机器波动大，但可以跑短基准或输出趋势。我的原则是 PR CI 保正确性，定期任务保性能趋势，避免把不稳定性能数值变成噪音。

### Q16（频率：中）[后端/工程取舍]

**面试官问题**：如果让你继续迭代这个项目，你下一步会做什么？

**口播版（第一人称，≥150 字，完整 STAR）**

我会按“补正确性边界，再补性能收敛，最后补产品化能力”的顺序迭代。第一步是把 SCAN 扩展到 SSTable 历史数据，因为当前只覆盖热数据，和边缘查询的完整目标还有差距；实现时要做多源有序归并、墓碑处理和新旧版本去重。第二步是强化 compaction 调度和元数据内存控制，因为高频小值压测显示 heap 增长可能来自 SSTable 文件数、Bloom 和索引常驻。第三步是补 Raft 多节点故障注入，把选主、日志追赶和快照恢复测扎实。第四步是完善观测出口，把当前日志指标扩展到 HTTP 或 Prometheus。这样迭代不是继续堆功能，而是围绕当前系统已经暴露的边界，把它从项目型实现逐步推向更可信的工程原型。

**追问 1**：如果只能选一个方向，你会先做哪个？

**口播版（≥150 字）**：我会先做 SSTable 历史范围扫描，因为它直接关系到项目定位是否闭环。README 里强调边缘侧查询并只回传命中切片，但当前源码层面 Scan 只覆盖 MemTable 热数据，这意味着一旦数据刷盘，历史窗口查询能力就不完整。这个方向也能复用已有能力：SSTable 已经有块索引、Bloom 和有序迭代器，compaction 也有 K 路归并思路。实现时我会先做正确性版本：范围过滤候选文件，构建多源迭代器，和 active/dirty 合并，按最新版本和墓碑语义输出；再做性能优化，比如按块跳过和谓词执行成本控制。优先补这个，能让项目从“热窗口查询 demo”更接近“边缘切片查询引擎”。

**追问 2**：你认为这个项目面试时最有价值的表达方式是什么？

**口播版（≥150 字）**：我认为最有价值的表达不是堆名词，而是讲工程闭环。比如我不会只说“我实现了 LSM、Raft、WAL”，而是讲一个具体问题：PUT 只有 225 qps，我如何通过 GET 对照和 fsync 测量归因，如何用 group commit 修改写路径，如何保持写确认语义不变，最后用 benchmark 证明提升。再比如恢复 bug，我会讲如何用小阈值强制刷盘、重启复现、定位 MaxKey 错误、修复元信息加载并补测试。这样的表达能体现我不仅会写数据结构，还会发现反常、设计实验、控制风险、承认边界。对面试官来说，这比单纯列技术栈更能判断工程成熟度。

## 源码证据索引（必填）

| 主题 | 关键路径与内部符号 | 对应正文位置 |
|---|---|---|
| 项目定位与能力 | `README.md` | Q1、Q16 |
| standalone 写路径 | `service/fsm.go`：`KVServer.Write`、`writeStandalone`、`applyStandalone` | Q2、Q7 |
| WAL group commit | `storage/wal.go`：`flushLoop`、`drainInto`、`commit`、`Append` | Q3 |
| WAL 重放与原子重写 | `storage/wal.go`：`Replay`、`Rewrite`、`doRewrite` | Q2、Q7 |
| 性能排障文档 | `docs/iteration-2026-07-19-standalone-write-durability.md` | Q3、Q6、Q15 |
| MemTable 跳表与双表 | `storage/zstorage/memtable.go`：`Put`、`Get`、`Delete`、`Flush`、`ScanRange` | Q4、Q5、Q8、Q12 |
| SSTable 文件格式与读路径 | `storage/zstorage/SSTable.go`：`WriteToSSTable`、`ReadFromSSTable`、`searchBlock`、`LoadSSTableMetaList` | Q4、Q6、Q9 |
| 分区 Bloom Filter | `storage/zstorage/partitioned_bloom.go` | Q9、Q10 |
| Compaction 归并 | `storage/zstorage/merge_iterator.go`、`storage/zstorage/iterator.go` | Q10、Q16 |
| 背压组件 | `pkg/credit/credit.go`、`storage/zstorage/memtable.go`：`acquireCredit`、`InflightBytes` | Q8、Q15 |
| 高频摄入压测 | `docs-step/M1-ingest-benchmark-result.md`、`docs-step/M2-backpressure-result.md` | Q8、Q15 |
| 采集入口过滤 | `service/ingesthook/filter.go`：`Filter.Handle`、`parsePut`、`redact` | Q11 |
| 路由与丢弃响应 | `service/router.go`：`PreHandle`、`sendDropped`、`handlePut`、`handleScan` | Q11、Q14 |
| Scan 协议与谓词 | `pkg/proto/scan.go`、`pkg/predicate/predicate.go`、`service/fsm.go`：`Scan` | Q12 |
| Raft 接入 | `Raft/raft.go`、`Raft/rpc.go`、`Raft/raft_wal.go`、`service/fsm.go`：`AppendEntry`、`WaitForCommit`、`Apply` | Q13 |
| 自研 TCP 入口 | `network/banNet/DataPack.go`、`network/banNet/msgHandle.go`、`network/banNet/server.go` | Q14 |
| 指标快照 | `pkg/metrics/metrics.go`、`Server/server.go` | Q15 |
